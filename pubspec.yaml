name: splash_smoke
description: "A Flutter package to create a splash screen with a smoke effect."
version: 0.0.3
homepage: "https://github.com/SwanFlutter/splash_smoke"

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:



